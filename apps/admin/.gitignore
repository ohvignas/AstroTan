node_modules
.DS_Store
dist
dist-ssr
*.local
# Not the blanket `.env*` the template started with — that also matched
# `.env.example`, which is meant to be committed (see
# packages/backend/.gitignore for the same distinction).
.env
.env.local
.env.*.local
.tanstack
.wrangler
.output
.vinxi
__unconfig*
todos.json

# typescript
*.tsbuildinfo

# regenerated by the TanStack Router Vite plugin on every dev/build run —
# unlike packages/backend/convex/_generated (which needs Convex credentials
# to regenerate), this needs nothing but the local route files.
src/routeTree.gen.ts
